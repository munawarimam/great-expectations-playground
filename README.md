## Great Expectations

### Make sure you already install
```
$ python3 -m venv venv
$ source venv/bin/activate
$ pip3 install -r requirements.txt
```

### Create GX project if you want start from beginning
`great_expectations init`

### Quick Start
`$ python3 main.py`
